<p id="notice"><%= notice %></p>

<h2><%= @job.description %></h2>

<h2><%= @job.origin %></h2>

<h2><%= @job.destination %></h2>

<h2><%= @job.cost %></h2>

<h2><%= @job.amount%></h2>


<% @job.boats.each do |boats| %>
<p><%= link_to boat.id, boat_path(boat) %></p>
<% end %>

<%= link_to 'Edit', edit_job_path(@job) %> |
<%= link_to 'Back', jobs_path %>
