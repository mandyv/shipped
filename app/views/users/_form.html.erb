<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h2> <%= @user.username %> </h2>

<h2><%= @user.username %>'s Boats </h2>

<% @user.boats.each do |post| %>
<p><%= link_to user.username, boat_path(boat) %></p>
<% end %>
  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
